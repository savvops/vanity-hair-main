import { defineCollection, z } from 'astro:content';

// Content collections are defined here
// Currently using src/content/site.json for all site content
// managed through Decap CMS

export const collections = {
  // Add new collections here as needed
};
